{
  "name": "file-rag-scanner",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --stream -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=22.16.0",
    "pnpm": ">=10.23.0"
  },
  "packageManager": "pnpm@10.23.0"
}
