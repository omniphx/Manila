Implement the work in the project plan found in `/plans`.

- Each github issue when completed should have a commit. You will need to spin up the frontend and backend
  on your own.
- The `mvp-1` branch is your `main` branch that you should merge into
- Create a PR for each github issue and link them.
- Each PR will need to be reviewed by the `pr-reviewer` and tested by the `qa-tester`
- Respond to all feedback in the PR before merging.
- Do not stop until all issues in the plan have been completed.
- After PR is complete update the plan file, compact the conversation and move on to the next task.
- PR titles/description should contain GH ticket number
- Close ticket after PR is merged.

$ARGUMENTS
